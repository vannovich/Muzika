





























































































































































































































































































































































































































































































































































































import React from "react";

function App() {
  let tracks = [
    { name: "abc", audio: "link1" },
    { name: "abc2", audio: "link2" },
  ];

  return (
    <div className="App">
      <h1>Vans-music</h1>
      {tracks.map((element) => {
        return (
          <div style={{ backgroundColor: "green" }}>
            <p>Name: {element.name}</p>
            <p>Link: {element.link}</p>
          </div>
        );
      })}
    </div>
  );
}

export default App;
'